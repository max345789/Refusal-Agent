# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/refusal_bot?schema=public"

# Redis (for BullMQ)
REDIS_URL="redis://localhost:6379"

# OpenAI
OPENAI_API_KEY=sk-your-openai-api-key

# Optional: API key for server-to-server (if needed)
INTERNAL_API_KEY=
