services:
  # Public marketing site (Next.js)
  - type: web
    name: refusal-bot-web
    runtime: node
    rootDir: web
    plan: free
    # Render sets NODE_ENV=production; ensure dev deps (tailwind/postcss) are installed for Next build.
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      # Set this to your deployed API base URL.
      - key: NEXT_PUBLIC_API_URL
        value: https://example-api.your-domain.com
      # Enable app routes for the self-serve MVP (onboarding -> dashboard).
      - key: NEXT_PUBLIC_ENABLE_APP
        value: "1"

  # API (Express + Prisma)
  - type: web
    name: refusal-bot-api
    runtime: node
    rootDir: server
    plan: free
    # One-off schema push during build to avoid paid shell; remove after first successful deploy if desired.
    buildCommand: npm ci && npm run db:generate && DATABASE_URL=$DATABASE_URL npx prisma db push --schema src/database/schema.prisma --accept-data-loss || exit 1
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      # Render injects PORT automatically.
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: INTERNAL_API_KEY
        sync: false
